# This pages controller was created in order to show the roots from routes

class PagesController < ApplicationController
	def home
	end
end

# If the pages controller doesnt know what to do, then the application controller probably will